
想找一个合适的入场时机，选股标准
比如入场距离MA60 距离
当前股票的是否处于均线多头空头
当前股票的行业是否处于多头和空头
当前股票的日线，周线、月线的macd处于哪一个区间是收益比较高的
每次盈利多少开始止盈，最终收益会比较大
盈利多少就开始不加仓了，这种数据，我需要得到的是这个
也就是这个框架搭建的是一个智能股票因子筛选系统，通过我给的选股和买卖策略，分析当前策略的问题，优化策略的参数，
后续我还会有更多的策略需要你进行回测



- [x] 止盈数字计算

  - [x] 固定仓位卖出
- [ ] trade_summary  新增每笔的买卖记录

  - [ ] 校验是否买卖方式和策略是否一致

- [x] 优化算法，效率



  - [ ] 沪深 300 MACD 水上

- [ ] 仓位控制，排序买入
  - [ ] 择时，仓位根据环境变化
  - [ ] 沪深 300 MACD 水上

- [ ] 不同的配置同时回测，比较

  - [ ] 自动跑，网格

- [ ] 行业胜率信息汇总到一个新表里面，而不是目前只有前10
- [ ] 同一套参数在沪深300、中证500、中证1000上分别跑，如果只在一个池子赚钱说明过拟合了


- [ ] 找出亏得最多和最少的各十个，看能找规律吗

- [ ] 公司ROE
- [ ] MA60 ≤1% 直接满仓 优化魔数
- [ ] 持仓时若出现 >1% 阳线，则禁止后续加仓 ，优化魔数
- [ ] 开关COC 测试是否有影响
- [ ] 

回测流程
  1 拉取数据 2 计算指标 
  3 回测测数据集， 4 归因统计 优化参数 ，
  回测别的数据源，对比成果
  继续回测测试数据集


？？？ 
1. 周月区间0  dea< 0 

---

## 后续版本（来自 2026-05-06 brainstorm，等本期归因数据出来后再做）

### 下一版：选股打分 + 行业去重
- [ ] 新增 `src/scorer.py`：对当日触发必要条件的信号股做打分排序
  - 因子组合：动量分位 / 距 MA60 距离 / MACD 强度 / ROE 分位 / PE 反向分位 / 净利润增速 / 所属行业指数动量
  - 所有因子用全市场分位数（0~1）规避量纲问题
  - 权重不手工拍，由本期归因脚本输出指导初始值
- [ ] 行业去重：每日同一行业最多取 K 只（默认 K=1），进 config
- [ ] 每日新开仓上限 `max_new_positions_per_day`（默认 3）进 config
- [ ] `scorer_enabled` 开关：关闭时跳过打分 + 去重 + 每日上限，恢复"信号即买入"行为（用于持续归因）

### 下一版：大盘择时锁仓
- [ ] 沪深 300 数据 feed 接入（不参与策略 loop，仅作为大盘代理）
- [ ] `_is_market_ok()` 门槛函数，加在 `next()` 入场判断处
- [ ] 当前条件：`沪深300 DEA > 0`（单条件起步）
- [ ] 条件列表化结构 `timing_filters: [{type, params}, ...]`，多条件默认 AND
- [ ] `timing_filter_enabled` 开关
- [ ] 后续可加：沪深300 > MA200 / 沪深300 ATR 高波动门槛 / 沪深300 月线 MACD 等
- [ ] 持仓只锁新开仓，已有仓位的卖出逻辑不变

### 更后续：过拟合验证
- [ ] `tools/walk_forward.py`：滚动训练/测试窗口（训 2 年/测 1 年/步长 1 年）
  - 训练期网格搜索 Top-K 参数 → 测试期原封跑 → 看排名一致性
- [ ] `tools/param_sweep.py`：单参数扫描，画收益/夏普/回撤曲线
  - 判断标准：平滑高原（稳健）vs 尖峰（过拟合）
- [ ] 网格扫描参数候选：ATR 倍数、DEA lookback 天数、止盈区间上下限、scorer 各因子权重
- [ ] 前置条件：必须先有归因脚本输出的"有效因子清单"，否则在噪音上做网格

### 更后续：归因深化
- [ ] E-A Brinson 简化版：超额收益（vs 沪深300）拆成选股贡献 / 择时贡献 / 行业 beta 贡献